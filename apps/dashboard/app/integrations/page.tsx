export default function IntegrationsPage() {
  return (
    <main className="content">
      <section className="panel">
        <p className="eyebrow">Integrations</p>
        <h1>Spotify and academic connectors</h1>
        <p className="lede">
          Connector configuration, run history, and sanitized Spotify presence will land here with server-side auth
          guards and safe admin actions.
        </p>
      </section>
    </main>
  );
}
