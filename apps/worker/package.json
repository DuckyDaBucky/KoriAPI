{
  "name": "@kori/worker",
  "version": "0.2.0",
  "private": true,
  "description": "Background workers for KoriAPI job orchestration",
  "scripts": {
    "build": "tsc -b",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@kori/db": "0.1.0",
    "@kori/shared": "0.1.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
